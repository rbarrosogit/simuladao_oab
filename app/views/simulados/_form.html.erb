<%= form_with(model: simulado, local: true) do |form| %>
  <% if simulado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(simulado.errors.count, "error") %> prohibited this simulado from being saved:</h2>

      <ul>
        <% simulado.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
